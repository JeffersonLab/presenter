services:
  oracle:
    extends:
      file: docker-compose.yml
      service: oracle

  keycloak:
    extends:
      file: docker-compose.yml
      service: keycloak

  puppet:
    extends:
      file: docker-compose.yml
      service: puppet

  presenter:
    extends:
      file: docker-compose.yml
      service: presenter
    build:
      context: .
      dockerfile: Dockerfile